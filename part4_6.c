#include <stdio.h>

int main(){
    long long_neg=-10;
    long long_pos=10;

    printf("long_neg: %ld\n", long_neg);
    printf("long_pos: %ld\n", long_pos);

    unsigned long uns_long_neg=-1;
    printf("uns_long_nega: %lu\n", uns_long_neg);

    printf("size: %lu\n", sizeof(long_pos));


return 0;
}
