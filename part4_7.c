#include <stdio.h>

int main(){
    long long long_neg=-6000000000;
    long long long_pos=6000000000;

    printf("long_neg: %lld\n", long_neg);
    printf("long_pos: %lld\n", long_pos);

    unsigned long long uns_long_neg=-1;

    printf("uns_long_neg: %llu\n", uns_long_neg);

return 0;
}
